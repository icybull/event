const swiper = new Swiper('.swiper', {
    // Optional parameters
    direction: 'horizontal',
    loop: false,
    infinite: true,
    slidesPerView:'auto',
    spaceBetween:16,
    followFinger:true,
    freeMode:true,
    animating:true,
  });